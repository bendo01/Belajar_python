import pygame
import sys
import random

pygame.init()
pygame.mixer.init()  # Siap untuk sound effect nantinya

WIDTH, HEIGHT = 400, 500
screen = pygame.display.set_mode((WIDTH, HEIGHT))
pygame.display.set_caption("Tic Tac Toe PRO")

FONT = pygame.font.SysFont(None, 40)
FONT_SMALL = pygame.font.SysFont(None, 30)

WHITE = (255, 255, 255)
BLACK = (0, 0, 0)
RED = (255, 0, 0)
BLUE = (0, 100, 255)
GREEN = (0, 200, 0)
GRAY = (200, 200, 200)

board = [["" for _ in range(3)] for _ in range(3)]
player = "X"
game_over = False
winner_line = None
game_status_text = ""  # Untuk menampilkan info menang/seri di layar

mode = "MENU"  # MENU / LEVEL / GAME
ai_mode = False
ai_level = "EASY"  # Disamakan menjadi bahasa Inggris agar sinkron

def beep():
    # Menggunakan print untuk sementara, Anda bisa ganti dengan sound asli:
    # pygame.mixer.Sound("beep.wav").play()
    print("🔊 Beep")

def draw_menu():
    screen.fill(WHITE)